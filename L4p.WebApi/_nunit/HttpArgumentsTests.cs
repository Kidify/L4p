using NUnit.Framework;

namespace L4p.WebApi._nunit
{
    [TestFixture]
    class HttpArgumentsTests
    {
        [Test]
        public void populate_starting_from_test()
        {
            var args = HttpArguments.New("", null);

//            server.AddRoute("bm/client/event/{deviceId}/{sessionId}/{version}/{workflow}/{action}/{label}", module, self.TrackEvent);
//            var requestArgs = args.PopulateStaringFrom("event");

        }
    }
}